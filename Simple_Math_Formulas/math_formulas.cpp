#include <iostream>
using namespace std;

int main()
{
    // Area of a Circle: 2 A = πr 2(pi times radius squared)

        // Area of a Square (वर्ग का क्षेत्रफल) : A = s*s (side squared);
        // int area = 5;
        // cout << area * area << endl;

        // Area of a Rectangle (आयत का क्षेत्रफल) : A = l×w (length times width)
        // int l = 4,
        //     w = 5;
        // int areaOfRectangle = l * w;

        // cout << areaOfRectangle << endl;

        return 0;
}